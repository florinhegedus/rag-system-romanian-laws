// services/frontend/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Enables static exports
    distDir: 'build', // Optional: Set custom output directory
  };
  
  module.exports = nextConfig;